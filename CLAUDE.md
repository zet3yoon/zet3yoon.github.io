# 포트폴리오 웹사이트

## 프로젝트 개요

오승윤(Product Manager)의 개인 포트폴리오 웹사이트입니다. 이직 지원용으로 사용합니다.

목표 회사는 대기업급 IT 기업입니다. 네이버, 쿠팡, 토스, 버킷플레이스(오늘의집) 같은 테크 기업과 삼성, 현대 등 대기업 IT 직군을 함께 고려합니다.

## 콘텐츠 출처

내용의 원본은 Notion에 있습니다.

- 워크스페이스: The Outstanding
- 마스터 포트폴리오: https://app.notion.com/p/a641f09a9b118226aef3817dc5a1f566
- 프로젝트 데이터베이스: https://app.notion.com/p/7021f09a9b1182af976401486e990c3d

Notion MCP로 접근할 수 있습니다. 내용이 바뀌면 Notion을 원본으로 보고 사이트에 반영합니다.

관련 산출물 이미지가 아래 경로에 있습니다. 공개 여부는 아래 "주의사항" 참고.

```
...\local-agent-mode-sessions\...\local_85ce6709-ee49-4356-b3f0-04e8f63a396d\outputs\산출물_v1\블러\
```

## 기술 스택

순수 HTML + CSS + JavaScript. 프레임워크와 빌드 과정을 쓰지 않습니다.

이유는 두 가지입니다. 첫째, 이직용 포트폴리오는 PDF 출력 요구가 잦아 정적 HTML이 유리합니다. 둘째, 소유자가 웹 개발을 배우는 중이라 구조가 눈에 보이는 편이 낫습니다.

배포는 GitHub Pages를 예정합니다.

## 구조 방침

마스터 버전을 먼저 만들고, 지원 회사에 따라 변형 버전을 파생시킵니다.

- `index.html` — 마스터. 모든 내용 포함
- `index-<회사>.html` — 변형. 마스터를 복사해 섹션을 덜어내거나 순서를 바꿈

변형을 쉽게 만들기 위해 다음을 지킵니다.

- 각 섹션을 독립된 `<section>`으로 분리하고, 잘라내도 나머지가 깨지지 않게 합니다
- 클래스 이름을 일관되게 붙입니다
- CSS는 `style.css` 하나로 공유합니다. 변형 파일이 늘어도 디자인은 한 곳에서 관리합니다

변형이 5개를 넘어가면 내용을 데이터로 분리하는 구조(`data.js` + 렌더링)로 옮기는 것을 검토합니다. 그전에는 미리 추상화하지 않습니다.

## 모션 방침

스크롤 스토리텔링 수준으로 갑니다. 모션은 장식이 아니라 내용 이해를 돕는 용도로만 씁니다.

- 메인 페이지의 등장 효과와 숫자 카운트업은 `motion.js`에서 라이브러리 없이 구현합니다. `IntersectionObserver`와 `requestAnimationFrame`만 사용합니다.
- 프로세스 흐름도처럼 복잡한 순서 제어가 필요한 상세 페이지에서만 GSAP을 CDN으로 씁니다.
- About Me와 How I Work은 읽는 내용이므로 등장 효과를 걸지 않습니다. 모션이 독서를 방해하면 안 됩니다.

지켜야 할 제약이 셋 있습니다.

- `prefers-reduced-motion: reduce`가 켜져 있으면 모든 애니메이션을 끄고 최종 상태를 즉시 보여줍니다.
- 자바스크립트가 꺼진 환경에서 내용이 사라지면 안 됩니다. 숨김 상태는 `.js` 클래스가 붙었을 때만 적용합니다.
- `IntersectionObserver`가 동작하지 않는 환경에 대비해 `motion.js`에 4초 안전장치를 둡니다. 관찰이 정상이면 아무 일도 하지 않습니다.

## 상세 페이지 작성 기준

프로젝트 상세 페이지를 쓸 때 지킵니다.

- **성과와 배움을 함께 씁니다.** 배움만 남기면 무엇이 달라졌는지가 사라집니다. Notion 원문의 성과 항목을 빠뜨리지 않습니다.
- **강조는 수치와 판단이 갈린 구절에만 겁니다.** 문장 전체를 굵게 하면 아무것도 강조되지 않습니다.
- **서비스 기획자·PM의 판단이 앞에 오게 씁니다.** 디자인 콘셉트, 제작 과정, 프로모션 구성은 마케팅 서술로 읽히므로 비중을 줄이거나 종속절로 처리합니다. 남길 것은 "무엇을 보고 그렇게 결정했는가"입니다.
- **'채널'은 유통 채널(마트·백화점·온라인)을 뜻할 때만 씁니다.**
- **회원·멤버십 경험은 B2C가 아니라 시스템 관점으로 씁니다.** 고객 혜택과 회원 정책이 아니라 데이터 구조, 연동 규격, 운영 효율의 언어로 서술합니다. 지원 대상 직군(플랫폼·백엔드·어드민 기획)이 찾는 키워드는 데이터 흐름, 정규화·표준화, 정책 수립, 프로세스 개선, 업무 자동화, 어드민 고도화입니다. 경로·창구·계열사를 가리킬 때는 그 단어를 그대로 씁니다.
- **산출물 개수를 쓰지 않습니다.** "요건 12개 항목 정의", "화면정의서 11종", "리포트 200건"처럼 만든 개수를 세지 않습니다. 실제로 한 일은 그보다 많고, 개수는 성과가 아닙니다. "요건 정의", "화면정의서 작성"으로 씁니다. 다만 설계 결정 자체가 개수인 경우(쿠폰 데이터 모델 5종처럼 유형을 몇 개로 나눌지 정한 것)는 남깁니다.
- **역할은 단어형으로 씁니다.** 문장으로 풀지 않습니다. `프로젝트 총괄 · 회원 식별자 체계 · 가입/발급 프로세스`처럼 명사구를 `·`로 잇습니다. "설계했습니다", "정의했습니다" 같은 서술 동사는 뺍니다. 상세 페이지에서는 헤더 메타의 역할을 한 줄로 두고, 구체 내용은 본문 첫 섹션 **맡은 범위**(`.rolegrid`)에 넣습니다. 메타에 길게 쓰면 기간·협업 칸과 높이가 안 맞습니다. **메인 카드에는 역할을 넣지 않습니다.** 카드가 길어지고 목록 훑기가 어려워집니다.

## 카드 태그 기준

메인 카드의 태그(`.tag--topic`)는 **성과가 아니라 프로젝트 주제**입니다. 콘텐츠 해시태그처럼 "이 프로젝트가 무엇에 대한 일인가"를 답합니다. 수치와 성과는 상세 페이지 상단 `.outcomes`와 메인 상단 지표 타일에 둡니다.

카드당 4개, `#신규 서비스 구축`, `#데이터 모델링`, `#계열사 연동` 형태. 어휘를 **의도적으로 재사용**합니다. 같은 태그가 여러 카드에 반복돼야 채용 담당자가 강점을 패턴으로 읽습니다. 현재 `#신규 서비스 구축` 6회, `#회원 체계 설계`·`#계열사 연동`·`#운영 자동화` 각 2회.

**성과가 아닌 것을 성과처럼 쓰지 않습니다.** "개발 요청 없이 현업 운영", "벤더 백엔드 수정 0건", "장애 0건" 같은 문구는 성과가 아니라 설계 방식이거나, 검증되지 않은 0 주장입니다. 면접에서 반박당하면 포트폴리오 전체의 신뢰가 깎입니다. 특히 **오픈 이후 실제로 수정이 이어진 건을 "수정 0건"으로 쓰면 사실과 다릅니다.** 이런 경우 진짜 성과는 "이해관계가 다른 조직을 같은 기준으로 움직이게 해서 오픈시킨 것"입니다.

판단 기준은 하나입니다. **채용 담당자가 이 문구를 보고 무엇을 알게 되는가.** 알게 되는 것이 없으면 뺍니다.

## 콘텐츠 방침

프로젝트는 **문제 → 접근 → 결과** 순서로 서술합니다. 수치가 있으면 앞에 둡니다.

건축학과 출신이 PM으로 전환한 이력을 감추지 않습니다. 낯선 영역에 들어가 판단 기준을 세운 경험이 반복해서 증명되는 점을 강점으로 다룹니다.

개인 프로젝트 gritrail(트레일러닝 정보 채널)을 포함합니다. "시키지 않아도 하는 일"의 근거로 About 섹션 뒤에 배치합니다.

## 주의사항

**전화번호를 사이트에 넣지 않습니다.** Notion 원본에는 있지만, 웹사이트는 검색엔진과 크롤러에 노출됩니다. 연락 수단은 이메일과 링크로 한정합니다.

**롯데 관련 산출물의 공개 범위는 아직 확정되지 않았습니다.** 회사 규정 확인 전까지 화면정의서·분석자료 이미지를 사이트에 넣지 않습니다. 텍스트 서술 위주로 다룹니다.

## 대화 규칙

한글로 대화합니다.

### 답변은 압축해서 씁니다

소유자가 caveman 플러그인을 full 강도로 쓰고 있습니다. 훅이 주입하는 규칙은 대화가 길어지면 뒤로 밀려 효력이 약해지므로, 여기에 다시 적어 둡니다. 매 답변 전에 이 항목을 확인합니다.

- 군더더기를 버립니다. "물론이죠", "도움이 되셨으면", "~인 것 같습니다" 같은 완충 표현을 쓰지 않습니다.
- 문장 조각을 허용합니다. 완결된 문장으로 늘리지 않습니다.
- 장식용 표와 소제목을 남발하지 않습니다. 항목이 셋 이상이고 비교가 필요할 때만 표를 씁니다.
- 도구 호출을 예고하거나 중계하지 않습니다. 바로 실행하고 결과만 말합니다.
- 기술 용어, 코드, 명령어, 오류 문자열은 그대로 둡니다. 압축 대상은 말투이지 정보가 아닙니다.

### 압축하지 않는 경우

다음은 짧게 줄이면 손해이므로 그대로 풀어 씁니다.

- 소유자가 명시적으로 설명을 요청한 경우 ("설명해줘", "이게 뭔가요", "왜 그런가요"). 이때는 충분히 씁니다.
- 되돌릴 수 없는 작업의 확인, 보안 경고.
- 순서가 중요한 다단계 절차. 조각으로 쓰면 순서를 오해할 수 있습니다.

보고와 결과 요약은 설명이 아닙니다. 압축 대상입니다.

### 그 외

- 소유자는 Git과 Claude Code를 처음 사용합니다. 새로운 개념이나 명령이 나오면 무엇을 왜 하는지 먼저 설명합니다. 이 설명은 위 예외에 해당합니다.
- 파일을 수정하거나 명령을 실행하기 전에, 그 동작이 무엇을 바꾸는지 알립니다.

## 작업 환경

- OS: Windows 11
- 셸: PowerShell 5.1 — JSON 등 설정 파일을 쓸 때 BOM이 붙지 않도록 주의합니다. `Out-File -Encoding utf8`은 BOM을 붙이므로 `[System.IO.File]::WriteAllText`에 `UTF8Encoding($false)`를 사용합니다.
- 이 폴더는 OneDrive 밖에 있습니다. 동기화 충돌을 피하기 위한 선택이므로 OneDrive로 옮기지 않습니다.
